services:
  marzban:
    image: marzban:local
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_COMMIT_SHA: auto
    restart: always
    env_file: .env
    network_mode: host
    volumes:
      - /var/lib/marzban:/var/lib/marzban
